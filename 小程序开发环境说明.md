# 微信小程序开发环境说明

> 本项目支持放在任意盘符和任意目录中。文档中的项目根目录统一记为 `<Parking>`。

## 1. 微信开发者工具

需要安装微信开发者工具，并在「设置 → 安全设置」中开启服务端口。

脚本会自动查找以下默认安装位置：

```text
%ProgramFiles(x86)%\Tencent\微信web开发者工具
%ProgramFiles%\Tencent\微信web开发者工具
```

如果本机安装在其他位置，设置环境变量后即可使用：

```powershell
[Environment]::SetEnvironmentVariable(
  "WECHAT_IDE_DIR",
  "D:\Apps\微信web开发者工具",
  "User"
)
```

重新打开命令行窗口后生效。

## 2. 项目目录

当前小程序项目目录为：

```text
<Parking>\parking-miniapp
```

不要把项目目录写成固定的 `I:\ChatGPT\Parking` 或 `L:\Chat GPT\Parking`。根目录下和子目录中的部署脚本都会根据脚本自身位置自动计算路径，因此换盘符、换电脑、目录中包含空格都可以正常运行。

首次打开项目时，在微信开发者工具中导入：

```text
<Parking>\parking-miniapp
```

## 3. 常用操作

在项目根目录双击：

```text
一键上传云函数.bat
```

也可以使用：

```text
deploy\cloud-deploy.bat
```

启动本地后端：

```text
server\启动后端服务.bat
```

所有脚本默认使用 PATH 中的 `node`。如果 Node.js 没有加入 PATH，可以为当前电脑设置：

```powershell
[Environment]::SetEnvironmentVariable(
  "PARKING_NODE_EXE",
  "D:\Tools\nodejs\node.exe",
  "User"
)
```

## 4. 项目共用配置与电脑私有配置

两台电脑可以共用：

- Git 仓库中的代码和数据文件
- 小程序 AppID
- 云开发环境 ID
- 云函数名称

每台电脑应独立维护：

- `WECHAT_IDE_DIR`、`PARKING_NODE_EXE`
- 局域网 IP 和本地服务端口
- 微信开发者工具登录状态
- 代码上传密钥
- 本地 SQLite 数据库和缓存

云函数部署脚本支持以下环境变量覆盖默认值：

```text
WECHAT_IDE_DIR
PARKING_NODE_EXE
PARKING_PROJECT
PARKING_CLOUD_ENV
PARKING_CLOUD_FUNCTION
PARKING_APPID
PARKING_UPLOAD_KEY_DIR
PARKING_IDE_PORT
```

上传密钥放在 `server\keys` 下，仅保存在本机，不能提交到 Git。

## 5. 两台电脑切换流程

第一次使用时分别克隆仓库并安装 Node.js、微信开发者工具。之后切换电脑前后执行：

```powershell
git pull --rebase origin main
```

完成修改后提交：

```powershell
git add .
git commit -m "说明本次修改"
git push origin main
```

注意：云开发数据库是两台电脑共用的；`server\parking.db` 是本地数据库，不会因为 Git 拉取而自动同步。
