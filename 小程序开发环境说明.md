# 微信小程序开发环境说明

> 准备时间：2026-09-09

## 1. 微信开发者工具

| 项目 | 内容 |
| --- | --- |
| 版本 | 稳定版 Stable **2.02.2608070**（2026/09/07 发布，基于 Electron 36.6 / Chromium 136） |
| 安装路径 | `C:\Program Files (x86)\Tencent\微信web开发者工具` |
| 主程序 | `微信开发者工具.exe` |
| 命令行入口 | `C:\Program Files (x86)\Tencent\微信web开发者工具\cli.bat` |
| 安装包备份 | `I:\ChatGPT\Parking\_installers\wechat_devtools_2.02.2608070_win32_x64.exe`（182MB，静默安装参数 `/S`） |

### 常用 CLI 命令（需先开启服务端口）

```bash
# 打开项目
cli.bat open --project "I:\ChatGPT\Parking\miniapp"
# 构建 npm
cli.bat build-npm --project "I:\ChatGPT\Parking\miniapp"
# 预览 / 上传
cli.bat preview --project "I:\ChatGPT\Parking\miniapp"
cli.bat upload --project "I:\ChatGPT\Parking\miniapp" -v 1.0.0 -d "说明"
```

**开启服务端口**：IDE 顶部菜单 → 设置 → 安全设置 → 服务端口 → 打开。
未开启时 CLI 会报 `IDE service port disabled`。

## 2. 项目脚手架

路径：`I:\ChatGPT\Parking\miniapp`

```
miniapp/
├── app.js                  # 全局逻辑
├── app.json                # 全局配置（页面注册、窗口样式）
├── app.wxss                # 全局样式（已引入 TDesign 主题样式）
├── project.config.json     # 项目配置（appid=touristappid 测试号）
├── sitemap.json
├── package.json            # 依赖：tdesign-miniprogram
├── pages/
│   └── index/              # 首页（组件库验证页）
│       ├── index.js/.json/.wxml/.wxss
├── node_modules/
└── miniprogram_npm/        # npm 构建产物（小程序实际读取的目录）
    └── tdesign-miniprogram/
```

### 技术栈

- 原生 JS 框架（无 TS、无跨端框架）
- **TDesign 小程序版 1.16.0**（腾讯官方组件库，104 个组件）
- AppID：`touristappid`（测试号，本地开发够用；注册小程序后在 `project.config.json` 里换成正式 AppID）

### 首次打开项目

IDE → 导入项目 → 目录选 `I:\ChatGPT\Parking\miniapp` → AppID 选「测试号」→ 导入。

## 3. TDesign 使用方式

页面 `.json` 中按需引入：

```json
{
  "usingComponents": {
    "t-button": "tdesign-miniprogram/button/button",
    "t-cell": "tdesign-miniprogram/cell/cell"
  }
}
```

全局样式已在 `app.wxss` 引入：

```css
@import 'miniprogram_npm/tdesign-miniprogram/common/style/index.wxss';
```

注意：`app.json` 中不要加 `"style": "v2"`，v2 基础样式会干扰组件库显示。

### 重新构建 npm

以后新增/升级依赖后，二选一：

1. IDE 菜单：工具 → 构建 npm
2. 手动同步（本次采用的方式）：
   ```bash
   cp -r node_modules/<包名>/miniprogram_dist/. miniprogram_npm/<包名>/
   ```

## 4. 环境现状

| 工具 | 版本 |
| --- | --- |
| Node.js | v22.22.2（WorkBuddy 托管） |
| npm | 10.9.7 |
| Python | 3.13.14 |
| PC 微信 | 已安装（D 盘），可用于扫码登录 |

## 5. 已知待办

- [ ] IDE 中开启「服务端口」，以便使用 CLI 命令
- [ ] 注册小程序账号后替换正式 AppID
- [ ] 开启「不校验合法域名」（已在 project.config.json 设置 `urlCheck: false`，调试接口需要）
