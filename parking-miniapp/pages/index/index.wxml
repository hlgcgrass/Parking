<view class="page tab-panel" hidden="{{activeTab !== 0}}">
  <include src="./home.wxml" />
</view>

<view class="tab-panel" hidden="{{activeTab !== 1}}">
  <include src="./nearby.wxml" />
</view>

<view class="tab-panel" hidden="{{activeTab !== 2}}">
  <include src="../mine/mine.wxml" />
</view>
